FROM mysql:8.4

# Set environment variables for database, user, and password
ENV MYSQL_DATABASE=test_db
ENV MYSQL_USER=test_user
ENV MYSQL_PASSWORD=TestPass123!
ENV MYSQL_ROOT_PASSWORD=RootPass123!

# No need for a custom CMD; the official image will create the DB and user automatically

# docker build -f Dockerfile.mysql -t gosqlapi-test-mysql .
# docker run --name gosqlapi-test-mysql -p 13306:3306 -d gosqlapi-test-mysql
